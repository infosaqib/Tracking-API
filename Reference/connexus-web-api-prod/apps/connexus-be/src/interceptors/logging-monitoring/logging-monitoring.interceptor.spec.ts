import { LoggingMonitoringInterceptor } from './logging-monitoring.interceptor';

describe('LoggingMonitoringInterceptor', () => {
  it('should be defined', () => {
    expect(new LoggingMonitoringInterceptor()).toBeDefined();
  });
});
